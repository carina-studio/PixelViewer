# PixelViewer 2026 中有什麼改變
 ---

## 新功能
+ 支援於觸控螢幕及觸控板上以雙指縮放影像。
+ 支援水平和垂直翻轉影像。

## 改善
+ 改善影像檢視器的使用者體驗。
+ 改善對 `EXIF`/`TIFF` 方向中繼資料的讀取，可正確處理所有方向值（包含編碼翻轉的值）。

## 行為變更
+ 點兩下影像時於 `調整至視窗大小` 與 `縮放至 100%` 之間切換。

## 錯誤修正
+ 其他錯誤修正。